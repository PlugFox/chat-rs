# Changelog

## [0.1.0] - 2026-03-19

### Added
- Project initialization: workspace with `chat_protocol`, `chat_client`, `chat_server`, `xtask`
- `chat_protocol`: frame types, binary codec, error codes, domain types (permissions, roles, etc.)
- `xtask`: check, fmt, test commands
- Documentation: architecture, protocol, codec, error-codes, database, server, client, milestones
- Server config example (`config/config.example.toml`)
